//Write a C program to create a variable side = 8 and calculate the area of a square.
#include <stdio.h>
int  main()
{
    int side = 8;  // Predefined value
    int area = side * side;  // Calculate area
    printf("The area of the square with side %d is: %d\n", side, area);
    return 0;
}